<template>
  <PageHeader sticky :title="$t('craftable-pro', 'Create Club Page')">
    <Button
      :leftIcon="ArrowDownTrayIcon"
      @click="submit"
      :loading="form.processing"
      v-can="'craftable-pro.club-page.create'"
    >
      {{ $t("craftable-pro", "Save") }}
    </Button>
  </PageHeader>

  <Form :form="form" :submit="submit"  />
</template>

<script setup lang="ts">
import { ArrowDownTrayIcon } from "@heroicons/vue/24/outline";
import { PageHeader, Button } from "craftable-pro/Components";
import { useForm } from "craftable-pro/hooks/useForm";
import Form from "./Form.vue";
import type { ClubPageForm } from "./types";
import dayjs from 'dayjs'



interface Props {
  
}

const props = defineProps<Props>();

const { form, submit } = useForm<ClubPageForm>(
    {
          title: "", 
content: "", 
slug: "", 
published_at: "", 
craftable_pro_users_id: "", 
club_id: ""
    },
    route("craftable-pro.club-pages.store"),
    "post"
);
</script>
