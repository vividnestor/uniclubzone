import { QRL, Signal } from "@builder.io/qwik";
import { AutoAnimateOptions, AutoAnimationPlugin } from "../index";
/**
 * AutoAnimate hook for adding dead-simple transitions and animations to qwik.
 * @param options - Auto animate options or a plugin
 * @returns
 */
export declare function useAutoAnimate<T extends HTMLElement>(options?: Partial<AutoAnimateOptions> | AutoAnimationPlugin): [
    Signal<T | undefined>,
    QRL<(enabled: boolean | ((isEnabled: boolean) => boolean)) => void>
];
